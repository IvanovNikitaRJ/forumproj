import React from 'react';
import NavigationBar from "../components/UI/NavigationBar";

const Board = () => {
    return (
        <div>
            <NavigationBar/>
           BOARD
        </div>
    );
};

export default Board;